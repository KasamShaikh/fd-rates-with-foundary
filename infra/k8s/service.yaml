apiVersion: v1
kind: Service
metadata:
  name: fd-rates-api
  namespace: __NAMESPACE__
spec:
  type: ClusterIP
  selector:
    app: fd-rates-api
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
