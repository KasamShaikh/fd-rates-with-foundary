apiVersion: v1
kind: Service
metadata:
  name: fd-rates-web
  namespace: __NAMESPACE__
spec:
  type: LoadBalancer
  selector:
    app: fd-rates-web
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
