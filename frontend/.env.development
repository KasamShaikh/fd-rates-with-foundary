# Local dev: React dev server (:3000) → Flask backend (:7071)
# In production this is set at build time to the Container App FQDN, e.g.
#   REACT_APP_API_BASE_URL=https://fdrates-api.<region>.azurecontainerapps.io
REACT_APP_API_BASE_URL=http://localhost:7071
