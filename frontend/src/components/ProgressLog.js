import React, { useEffect, useRef } from 'react';/** * Live activity feed for the currently-running scrape. * Polls /api/scrape/progress every ~1.5s while `active` is true. */function ProgressLog({ events, active, onDone }) {  const logEndRef = useRef(null);  useEffect(() => {    // Auto-scroll to the bottom when new events arrive    if (logEndRef.current) {      logEndRef.current.scrollTop = logEndRef.current.scrollHeight;    }  }, [events]);  if (!active && (!events || events.length === 0)) return null;  return (    <div className="progress-log-card">      <div className="progress-log-header">        <span>          {active ? (            <>              <span className="pulse-dot" /> Live activity            </>          ) : (            <>Γ£à Activity log</>          )}        </span>        {!active && onDone && (          <button className="progress-dismiss" onClick={onDone} title="Dismiss log">            Γ£ò          </button>        )}      </div>      <div className="progress-log-body" ref={logEndRef}>        {events.length === 0 ? (          <div className="progress-log-empty">Waiting for the first step...</div>        ) : (          events.map((ev, i) => (            <div key={i} className={`progress-line lvl-${ev.level || 'info'}`}>              <span className="progress-ts">{ev.ts}</span>              {ev.bank && <span className="progress-bank">{ev.bank}</span>}              <span className="progress-msg">{ev.message}</span>            </div>          ))        )}      </div>    </div>  );}export default ProgressLog;