import jsond = json.load(open("_local_results/latest.json", "r", encoding="utf-8"))print(f"Scraped at : {d['scraped_at']}")print(f"Banks      : {d['bank_count']}")print(f"DI pages   : {d.get('di_pages', 0)}")tu = d.get("token_usage", {})print(    f"Tokens     : prompt={tu.get('prompt_tokens')} completion={tu.get('completion_tokens')} total={tu.get('total_tokens')}")print("--- Per-bank ---")ok = fail = 0for r in d["results"]:    name = r.get("bank_name", "?")    if r.get("error"):        fail += 1        reason = (r.get("reason") or "")[:110]        print(f"FAIL  {name:<42} {reason}")    else:        ok += 1        cats = r.get("categories") or []        rates = sum(len(c.get("rates") or []) for c in cats)        print(f"OK    {name:<42} categories={len(cats):<2} rates={rates}")print(f"\nSummary: {ok} OK / {fail} FAIL out of {len(d['results'])}")